export enum Efavorite {
  SEARCH_RESULT = `검색 결과`,
  FAVORITE = `즐겨찾기`,
}

export enum EnodeGubun {
  DUMMY = `T`,
  GROUP = `G`,
  FAVORITE_USER = `U`,
  ORGANIZATION_USER = `P`,
}

export enum EuserState {
  online,
  absence,
  otherWork,
  workingOutside,
  onCall,
  atTable,
  inMeeting,
  offline,
}

export enum EconnectType {
  windows = 1,
  android = 4,
  iphone = 6,
  mac = 10,
}
