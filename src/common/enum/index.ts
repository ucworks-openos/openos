export enum Efavorite {
  SEARCH_RESULT = `검색 결과`,
  FAVORITE = `즐겨찾기`,
  MY_PROFILE = `내 프로필`,
  MY_KEY = `MY_KEY`,
}

export enum Egubun {
  DUMMY = `T`,
  GROUP = `G`,
  FAVORITE_USER = `U`,
  ORGANIZATION_USER = `P`,
}

export enum EconnectType {
  windows = 1,
  android = 4,
  iphone = 6,
  mac = 10,
}
